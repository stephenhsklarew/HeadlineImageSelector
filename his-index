#!/usr/bin/env python3
"""Wrapper script for his-index command"""
import sys
import os

# Add src to path
sys.path.insert(0, os.path.join(os.path.dirname(__file__), 'src'))

from headline_image_selector.cli.index import main

if __name__ == "__main__":
    main()
